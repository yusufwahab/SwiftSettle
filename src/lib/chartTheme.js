// Recharts needs literal color values (SVG attributes don't resolve CSS
// custom properties), so these mirror the tokens defined in index.css.
// "primary/border/muted" are for the light marketing pages; the rest match
// the dark app-shell palette.
export const chartColors = {
  primary: "#1d3557",
  border: "#e4e7ec",
  muted: "#667085",

  accent: "#3b82f6",
  accent2: "#22c55e",
  gridDark: "#ffffff1a",
  mutedDark: "#98a2b3",
};
